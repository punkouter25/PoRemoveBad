# azure.yaml defines the Azure Developer CLI (azd) environment
name: PoRemoveBad
metadata:
  template: PoRemoveBad@0.0.1-beta
services:
  web:
    project: ./PoRemoveBad.Server
    language: dotnet
    host: appservice
